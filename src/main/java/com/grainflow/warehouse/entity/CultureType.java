package com.grainflow.warehouse.entity;

// Grain culture types — extend this enum when new cultures are added
public enum CultureType {
    WHEAT,
    BARLEY,
    CORN,
    SUNFLOWER,
    SOYBEAN,
    RYE,
    OATS
}
